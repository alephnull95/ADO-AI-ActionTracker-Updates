# M365 Onboarding Actions update feed

This public repository contains non-sensitive version metadata only. Application packages and internal Azure DevOps snapshots are stored in an access-controlled Microsoft SharePoint location.